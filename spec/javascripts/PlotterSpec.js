describe("Graph.Plotter", function() {

  var plotter;

  beforeEach(function() {
    plotter = new Graph.Plotter("element");
  });

  it("should plot the graph with initial call", function() {
    // var mockGraph = function() { console.log("faker") };
    // spyOn($, "plot").andReturn(mockGraph);
    // plotter.plot("");
  });

});
